# Local session

You are working directly on a local repository. No git operations are required
unless the goal explicitly asks for a commit.

Focus on understanding, editing, and validating the code. Use `run` to execute
tests and commands. Call `done` with a clear summary when the task is complete.
